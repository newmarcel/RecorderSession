language: objective-c
osx_image: xcode11.2
rvm:
- 2.6
install:
  - make init
script:
  - make test
after_success:
  - slather
  - bash <(curl -s https://codecov.io/bash) -f build/Reports/cobertura.xml -X coveragepy -X gcov -X xcode
